# .readthedocs.yaml
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"  # ou la version que tu utilises

sphinx:
  configuration: source/conf.py  # Chemin vers ton fichier conf.py

python:
  install:
    - requirements: requirements.txt  # ou docs/requirements.txt
